import os


# def pretty_print(dir_path):
#     if not os.path.isdir(dir_path):
#         raise NotADirectoryError('PATH '{path}' is not a directory or '{path}' not exists'.format(path=dir_path))
# pretty_print('1')
# print('{a}'.format(a=1))

print("PATH {path} is not a directory or '{path}' not exists".format(path='a'))


class A:
    def a(self):
        return 1
